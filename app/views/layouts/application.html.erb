<!DOCTYPE html>
<html data-ng-app="opinionationApp">
<head>
	<meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <title>OpinioNation</title>
  <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.0/css/bootstrap-glyphicons.css" rel="stylesheet">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag 'favicon.png' %>
</head>
<body>

<%= render 'partials/nav' %>

  <% if current_user %>
    <span style="display: block; color: white;"><%= "Hello, #{current_user.fname}!" %> <%= link_to "Sign out", session_path, method: :delete, id: "signzout" %></span>

  <% else %>
    <span style="display: block; color: white;"><%= link_to "Sign in", new_session_path %></span>
  <% end %>

<%= yield %>

  <!-- <span id="spin-loader"></span> -->

</body>
</html>
